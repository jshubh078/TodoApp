import { OperatorsConfig, FieldExpression } from '../helpers';
export default function finalizeFieldExpression(operators: OperatorsConfig, it: FieldExpression): {
    args: any[];
    type: "FieldExpression";
    operator: string;
};
